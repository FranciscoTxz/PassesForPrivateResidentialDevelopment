info:
  name: get count pending passes (admin)
  type: http
  seq: 7

http:
  method: GET
  url: "{{URL}}/passes/pending/count"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

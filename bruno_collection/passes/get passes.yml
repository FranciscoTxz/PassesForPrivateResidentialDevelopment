info:
  name: get passes
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/passes"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: get profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/profile"
  headers:
    - name: Authorization
      value: "{{TOKEN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

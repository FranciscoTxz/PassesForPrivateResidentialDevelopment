info:
  name: get user by house
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/users/house?house_id=SV101"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  params:
    - name: house_id
      value: SV101
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: delete user
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{URL}}/users/delete?email=user_1@hot.coms"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  params:
    - name: email
      value: user_1@hot.coms
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

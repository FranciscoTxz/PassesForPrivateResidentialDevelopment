info:
  name: get house
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/houses/SV401"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

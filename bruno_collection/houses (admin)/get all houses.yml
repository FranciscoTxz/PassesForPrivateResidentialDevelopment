info:
  name: get all houses
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/houses"
  headers:
    - name: Authorization
      value: "{{TOKEN_ADMIN}}"
  params:
    - name: limit
      value: "5"
      type: query
      disabled: true
    - name: next_cursor
      value: SV103
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
